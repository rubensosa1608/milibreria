// src/components/myFavourite/MyFavouriteBorder.stories.tsx

import { Canvas, Meta } from "@storybook/blocks";
import * as MyFavouriteBorder from "./myFavouriteBorder.stories"; // Asegúrate de que esta ruta sea correcta

<Meta of={MyFavouriteBorder} title="MyFavouriteBorder" />

# MyFavouriteBorder

Componente personalizado que muestra un ícono de favorito bordeado (FavoriteBorderIcon).

#### Example

<Canvas of={MyFavouriteBorder.Primary} />

## Usage

```tsx
import { MyFavouriteBorder } from 'milibreria';

function Example() {
  return (
    <MyFavouriteBorder
      color="primary"
      disabled={false}
      onClick={() => alert('Favorite Border clicked!')}
    />
  );
}

export default Example;
