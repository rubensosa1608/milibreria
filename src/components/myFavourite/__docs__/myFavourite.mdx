// src/components/myFavourite/MyFavourite.stories.tsx

import { Canvas, Meta } from "@storybook/blocks";
import * as MyFavourite from "./myFavourite.stories"; // Asegúrate de que esta ruta sea correcta

<Meta of={MyFavourite} title="MyFavourite" />

# MyFavourite

Componente personalizado de favorito (IconButton con ícono de corazón).

#### Example

<Canvas of={MyFavourite.Primary} />

## Usage

```tsx
import { MyFavourite } from 'milibreria';

function Example() {
  return (
    <MyFavourite
      color="primary"
      disabled={false}
      onClick={() => alert('Favorite clicked!')}
    />
  );
}

export default Example;
