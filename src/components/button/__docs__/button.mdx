import { Canvas, Meta } from "@storybook/blocks";
import Example from "./example.tsx";
import * as CustomButton from "./button.stories.tsx";

<Meta of={CustomButton} title="CustomButton" />

# CustomButton

Componente de botón con diferentes especializaciones.

#### Example

<Canvas of={CustomButton.Primary} />

## Usage

```ts

import {CustomButton} from 'milibreria';

function Example(){
 return (
 <CustomButton
 text: "Button primary",
 txtcolor: 'black',
 bgcolor: 'red',
 disabled: false,
 size: "small",
 onClick: () => alert("Button Primary"),
 />
 );
};

export default Example;
``` 

#### Example

<Canvas of={CustomButton.Secondary} />

## Usage

```ts
import {CustomButton} from 'milibreria';

function Example(){
 return (
 <CustomButton
 text: "Button secondary",
 txtcolor: 'white',
 bgcolor: 'pink',
 disabled: false,
 size: "small",
 onClick: () => alert("Button Secondary"),
 />
 );
};